"""Extraction logic for di-processor.

Pure(ish) extraction algorithms: page rendering/splitting, DI OCR orchestration,
LLM reconstruction of handwritten fields, merge into the combined extraction, and
the Pydantic schemas + validation for each stage's output.
"""
