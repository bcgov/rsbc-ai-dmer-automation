APP_CONFIGURATION_ENDPOINT=
SERVICE_BUS_NAMESPACE_FQDN=
POSTGRES_HOST=

# External Azure OpenAI (separate AI Hub subscription — not provisioned by this repo).
# Values are Key Vault secret references, not literal secrets — see
# docs/architecture/repository-design.md §10, §13 (item 8).
AZURE_OPENAI_ENDPOINT_KV_SECRET_NAME=
AZURE_OPENAI_API_KEY_KV_SECRET_NAME=
AZURE_OPENAI_DEPLOYMENT_NAME=
