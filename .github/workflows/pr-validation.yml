
# pr-validation.yml
#
# Purpose: Aggregate required status checks (build, test, lint, bicep-validate, security-scan) for PR gating
#
# NOTE: This is a structural placeholder. Implementation (steps, actions,
# matrix strategy per service) is intentionally deferred until the
# repository skeleton is approved. See docs/deployment/deployment-guide.md
# for the intended trigger/approval flow.

name: Pr Validation

on:
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "TODO: implement Aggregate required status checks (build, test, lint, bicep-validate, security-scan) for PR gating"'
