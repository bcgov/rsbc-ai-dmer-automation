
# build.yml
#
# Purpose: Build/package every changed service (Functions & Container Apps) on push and PR
#
# NOTE: This is a structural placeholder. Implementation (steps, actions,
# matrix strategy per service) is intentionally deferred until the
# repository skeleton is approved. See docs/deployment/deployment-guide.md
# for the intended trigger/approval flow.

name: Build

on:
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "TODO: implement Build/package every changed service (Functions & Container Apps) on push and PR"'
