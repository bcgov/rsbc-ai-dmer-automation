
# Dev seed data

Synthetic, non-PII seed data for local/dev environments only (never test or prod). Used to
exercise the full pipeline (intake -> OCR -> normalization -> rules -> audit) without real
DMER content.
