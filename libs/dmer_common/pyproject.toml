[project]
name = "dmer-common"
version = "0.1.0"
description = "Shared library for RSBC DMER Automation services"
requires-python = ">=3.12"
# dependencies intentionally omitted from this skeleton

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
