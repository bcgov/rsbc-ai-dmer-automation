# API Contracts

OpenAPI specs for any HTTP-facing surface (the `intake-processor` webhook endpoint, the
`audit-service` read API, and inter-service activity calls made by `workflow-orchestrator`).
One `.yaml` file per API, named `<service>-openapi.yaml`.
